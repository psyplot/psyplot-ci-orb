description: "Setup linux environment and install miniconda"
parameters:
  install_location:
    description: "Install location for anaconda"
    type: string
    default: "~/miniconda3"
steps:
  - run:
      environment:
        INSTALL_TO: <<parameters.install_location>>
      name: Install conda
      command: <<include(scripts/install_miniconda.sh)>>